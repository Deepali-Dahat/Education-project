import { AllPlaceholderDirective } from './all-placeholder.directive';

describe('AllPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new AllPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
